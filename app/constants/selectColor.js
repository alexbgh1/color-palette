const MIN_STEP = 0;
const MAX_STEP = 12;
const GAP_STEP = 1;
// if step=0 then the result is the same three colors
// if step=1 then the result is three colors + 1 color between each color (total 5 colors)

const MAX_NAME_LENGTH = 12;
export { MIN_STEP, MAX_STEP, GAP_STEP, MAX_NAME_LENGTH };
